import React from 'react';
import { BrowserRouter, Route, Switch } from 'react-router-dom';
import { HOME, WOMEN, MEN, JEWELRY, SHOES, WOMENS_PRODUCT, MENS_PRODUCT } from '../../routes';
import './App.css';

export default function App() {
    return (
      <BrowserRouter>
        <Switch>
          
        </Switch>
      </BrowserRouter>
    );
}